@page
@model MainFarm.Pages.FarmRecord.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Inventory[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventory[0].Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventory[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventory[0].Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventory[0].Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Inventory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Guid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Guid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Guid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
